# OpenAI API key (required)
OPENAI_API_KEY=sk-your-key-here

# Path to CC-CEDICT dictionary file (default: cedict_ts.u8 in project root)
CEDICT_PATH=cedict_ts.u8

# Optional path for the LangGraph checkpoint database used for durable chat sessions
CHECKPOINT_DB_PATH=checkpoints.sqlite
